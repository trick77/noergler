name: CI

# Tests run on pull requests. Master does not run tests: it builds and pushes
# the image (see release.yaml).
on:
  pull_request:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Build + test
    runs-on: ubuntu-latest
    # The Go module lives in backend/; hack/ and coverage/ stay at the root, so
    # the two gate steps below override this back to the repo root.
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: noergler
          POSTGRES_PASSWORD: noergler
          POSTGRES_DB: noergler_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U noergler"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      # fetch-depth: 0 - hack/patch-coverage.sh diffs the branch against the PR
      # base, which needs real history. The default shallow checkout leaves the
      # base ref unresolvable and the gate exits 2.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      # go-version-file rather than a pinned version, so the toolchain cannot
      # drift from what the module itself declares.
      - uses: actions/setup-go@v7
        with:
          go-version-file: backend/go.mod
          cache: true
          # setup-go resolves the cache key from go.sum at the WORKSPACE root
          # by default, which no longer has one.
          cache-dependency-path: backend/go.sum
      # hack/patch-coverage.sh drives diff-cover; without it the patch gate
      # fails with "command not found" after the tests have already run.
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install diff-cover==10.3.0
        working-directory: .
      # From the repo root, so hack/*.go is covered too: it is outside the
      # module, so `go vet ./...` in backend/ never sees it, and CI itself runs
      # hack/strip-comment-lines.go.
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "::error::These files are not gofmt'd. Run: gofmt -w ."
            echo "$unformatted"
            exit 1
          fi
        working-directory: .
      - run: go vet ./...
      - run: go build ./...
      - run: mkdir -p ../coverage
      # The race detector needs cgo; this is independent of the image's
      # CGO_ENABLED=0 build.
      #
      # -coverpkg=./... attributes coverage across package boundaries: code
      # exercised only by another package's tests (the api tests drive review,
      # store and render) is otherwise reported as uncovered.
      - run: go test -race -covermode=atomic -coverpkg=./... -coverprofile=../coverage/backend.out ./...
        env:
          CGO_ENABLED: "1"
          NOERGLER_TEST_DSN: postgres://noergler:noergler@localhost:5432/noergler_test?sslmode=disable
      # Go exposes no line metric - `go tool cover` reports statements only - so
      # the line percentage comes from a Cobertura conversion of the profile.
      # That also merges the duplicate blocks -coverpkg emits, one set per test
      # binary, which a naive sum over the raw profile gets badly wrong.
      - run: go run github.com/boumenot/gocover-cobertura@v1.5.0 < ../coverage/backend.out > ../coverage/backend.xml
      # coverage-gate.test.sh exercises the gate's ui branch, which parses the
      # vitest summary with node. This repo is Go only, but the self-test covers
      # both branches, so node has to be on PATH for it.
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      # The gate scripts are load-bearing, so they are tested BEFORE they are
      # trusted. Ordering matters: a fail-closed regression in coverage-gate.sh
      # aborts the job at the gate step, and a self-test placed after it would
      # never run to explain why.
      - run: ./hack/coverage-gate.test.sh
        working-directory: .
      - run: ./hack/strip-comment-lines.test.sh
        working-directory: .
      # The floor gate runs first: when a PR drops the project below the floor
      # the absolute number is the more actionable failure, and reporting it
      # before patch coverage keeps the log readable.
      # Both gates resolve paths from the repo root, not backend/.
      - run: ./hack/coverage-gate.sh backend
        working-directory: .
      - name: Backend patch coverage
        run: ./hack/patch-coverage.sh "origin/${{ github.base_ref || 'master' }}"
        working-directory: .

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: backend/go.mod
          cache: false
      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13
          working-directory: backend

  ui:
    name: Build + test the dashboard
    runs-on: ubuntu-latest
    steps:
      # fetch-depth: 0 - hack/patch-coverage.sh diffs against the PR base.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pip install diff-cover==10.3.0
      - run: npm ci
        working-directory: ui
      # `npm run build` is `tsc -b && vite build`, so this is the typecheck
      # too. It also proves the SPA still lands in backend/web/dist, which is
      # what the Go embed reads.
      - run: npm run build
        working-directory: ui
      - run: make fe-coverage
      - name: UI patch coverage
        run: ./hack/patch-coverage.sh "origin/${{ github.base_ref || 'master' }}"

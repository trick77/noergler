# Secrets / per-deployment config
.env
teams.yaml

# Plans and other HTML deliverables
docs/plans/

# Build. Anchored, unlike the Go repo's version: a bare `noergler` matches any
# path component of that name at any depth, which would silently swallow one
# under archive/.
/backend/noergler
fakes
/bin/
coverage/

# Python, for the archived implementation under archive/
.venv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# IDEs
.vscode/
.idea/
*.iml
*.swp
*~

# Agent tooling
.claude/
.cursor/
.aider*
.codex/
.zed/
.windsurf/

# macOS
.DS_Store

# IntelliJ HTTP client: host, team secret and admin token, filled in locally (template: .example)
http/http-client.env.json
